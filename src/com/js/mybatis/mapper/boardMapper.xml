<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<sql id="search">
		<trim prefixOverrides="or">
			<foreach item="type" collection="types">
				<trim prefix="or">
					<choose>
						<when test="type == 'title'.toString()">
							board_title like concat('%', #{keyword}, '%')
						</when>
						<when test="type == 'content'.toString()">
							board_content like concat('%', #{keyword}, '%')
						</when>
						<when test="type == 'writer'.toString()">
							user_name like concat('%', #{keyword}, '%')
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>
	
	<sql id="order">
		order by
		<choose>
			<when test="sort=='recent'.toString()">
				board_id
			</when>
			<otherwise>
				board_read_count
			</otherwise>
		</choose>
		desc
	</sql>


	<select id="selectAll" resultType="boardDTO">
		select board_id, b.user_id, m.user_name, board_title, board_content, board_type
		from tbl_user m join tbl_board b
			on m.user_id = b.user_id
			order by board_id desc
			limit 0, 3
	</select>

	
	 
	 <select id="boardSelectAll" resultType="boardDTO">
		select board_id, board_title, board_content, board_creation_date, board_reply_date, board_type, board_status, m.user_id, m.user_name
		from tbl_user m join tbl_board b
		on m.user_id = b.user_id
	</select>
	<select id="getTotal" resultType="_int">
		select distinct count(board_id)
		from tbl_user m join tbl_board b
		<trim suffixOverrides="and"> 
			on m.user_id = b.user_id and
			<include refid="search"/>
		</trim>
	</select>
	
	<insert id="insert">
		insert into tbl_board
		(board_id, board_title, board_content, board_creation_date, board_reply_date, board_status, user_id)
		values(#{boardId}, #{boardTitle}, #{boardContent}, #{boardCreationDate}, #{boardReplyDate}, #{boardStatus}, #{userId})
	</insert>
	
	<update id="updateBoard">
		update tbl_board 
		set 
		where
	</update>
	
	<delete id="deleteAll">
		delete * from tbl_board;
	</delete>

	<select id="getTotal" resultType="_int">
		select distinct count(board_id)
		from tbl_user m join tbl_board b
			on m.user_id = b.user_id
	</select>
	
	<!-- <insert id="insert">
		insert into tbl_board
		(board_id, board_title, board_content, user_id)
		values(#{boardId}, #{boardTitle}, #{boardContent}, #{userId})
	</insert> -->
</mapper>


















