 function handleSubjectChange(selectElement) {
        var boardType = selectElement.value;
        
        if (boardType === "provider-square") {
            // 구직일 때의 처리
        } else if (boardType === "qna") {
            // 구인일 때의 처리
        }
    }